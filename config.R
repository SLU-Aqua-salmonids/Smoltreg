#
# Define common constants used in several files in the smolt data import process.
# #
##

min_k <- 0.5
max_k <- 1.5
#
#
#
# rdir <- paste0("SMOLTS_", river, "_", year)
# dbname <- paste0("smolt_trap_",river,"_",year,".sqlite")
minlength <- 100
maxlength <- 250
# Event  can be 0 = unknown, 1 = caught (but not marked), 2 = marked, 3 = recaptured
UNKNOWN <- 0
CAUGHT <- 1
MARKED <- 2
RECAPTURED <- 3
REMOVED <- 4

